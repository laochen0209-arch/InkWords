[1mdiff --git a/app/api/webhooks/afdian/route.ts b/app/api/webhooks/afdian/route.ts[m
[1mindex ed1bbe3..8c20d8b 100644[m
[1m--- a/app/api/webhooks/afdian/route.ts[m
[1m+++ b/app/api/webhooks/afdian/route.ts[m
[36m@@ -8,6 +8,10 @@[m
  * - 接收爱发电发来的 Webhook 请求[m
  * - 按照爱发电官方文档进行验签[m
  * - 支付成功后自动点亮用户 VIP 状态[m
[32m+[m[32m *[m[41m [m
[32m+[m[32m * 极其重要：[m
[32m+[m[32m * - 无论发生什么情况，方法的最后一行必须返回 { "ec": 200, "em": "success" }[m
[32m+[m[32m * - 不要在 try 块中间 return，不要 throw 任何异常[m
  */[m
 [m
 import { NextRequest, NextResponse } from 'next/server'[m
[36m@@ -32,149 +36,132 @@[m [mexport async function POST(request: NextRequest) {[m
 [m
     if (!afdianUserId || !afdianToken) {[m
       console.error('[Afdian Webhook] 缺少爱发电配置环境变量')[m
[31m-      return NextResponse.json([m
[31m-        { ec: 400, em: 'Missing configuration' },[m
[31m-        { status: 400 }[m
[31m-      )[m
[31m-    }[m
[31m-[m
[31m-    // ============================================================================[m
[31m-    // 2. 读取并解析请求体[m
[31m-    // ============================================================================[m
[31m-    const body = await request.text()[m
[31m-    let requestData[m
[31m-[m
[31m-    try {[m
[31m-      requestData = JSON.parse(body)[m
[31m-    } catch (parseError) {[m
[31m-      console.error('[Afdian Webhook] 请求体 JSON 解析失败:', parseError)[m
[31m-      return NextResponse.json([m
[31m-        { ec: 400, em: 'Invalid JSON' },[m
[31m-        { status: 400 }[m
[31m-      )[m
[31m-    }[m
[31m-[m
[31m-    console.log('[Afdian Webhook] 请求数据:', JSON.stringify(requestData, null, 2))[m
[31m-[m
[31m-    // ============================================================================[m
[31m-    // 3. 爱发电 Webhook 验签[m
[31m-    // ============================================================================[m
[31m-    const { ec, em, data, sign } = requestData[m
[31m-[m
[31m-    if (!sign) {[m
[31m-      console.error('[Afdian Webhook] 缺少签名参数')[m
[31m-      return NextResponse.json([m
[31m-        { ec: 400, em: 'Missing signature' },[m
[31m-        { status: 400 }[m
[31m-      )[m
[31m-    }[m
[31m-[m
[31m-    // 按照爱发电官方文档的验签规则：[m
[31m-    // 将 params（JSON字符串） + token 拼接后进行 MD5 加密[m
[31m-    // 注意：爱发电的 Webhook 数据结构可能因版本不同而有所变化[m
[31m-    const paramsStr = JSON.stringify(data)[m
[31m-    const signContent = paramsStr + afdianToken[m
[31m-    const computedSign = crypto[m
[31m-      .createHash('md5')[m
[31m-      .update(signContent, 'utf-8')[m
[31m-      .digest('hex')[m
[31m-[m
[31m-    console.log('[Afdian Webhook] 计算的签名:', computedSign)[m
[31m-    console.log('[Afdian Webhook] 收到的签名:', sign)[m
[31m-[m
[31m-    if (computedSign !== sign) {[m
[31m-      console.error('[Afdian Webhook] 签名验证失败')[m
[31m-      return NextResponse.json([m
[31m-        { ec: 403, em: 'Invalid signature' },[m
[31m-        { status: 403 }[m
[31m-      )[m
[31m-    }[m
[31m-[m
[31m-    console.log('[Afdian Webhook] 签名验证通过')[m
[31m-[m
[31m-    // ============================================================================[m
[31m-    // 4. 提取订单信息和用户 ID[m
[31m-    // ============================================================================[m
[31m-    const orderData = data[m
[31m-    const remark = orderData?.remark || orderData?.order?.remark[m
[31m-    const orderStatus = orderData?.status || orderData?.order?.status[m
[31m-[m
[31m-    console.log('[Afdian Webhook] 订单备注 (用户ID):', remark)[m
[31m-    console.log('[Afdian Webhook] 订单状态:', orderStatus)[m
[31m-[m
[31m-    if (!remark) {[m
[31m-      console.error('[Afdian Webhook] 缺少用户 ID (remark 字段为空)')[m
[31m-      return NextResponse.json({ ec: 200, em: 'success' })[m
[31m-    }[m
[31m-[m
[31m-    // ============================================================================[m
[31m-    // 5. 检查订单状态是否为支付成功[m
[31m-    // 爱发电订单状态：通常 2 表示支付成功[m
[31m-    // ============================================================================[m
[31m-    const successStatuses = [2, '2', 'paid', 'success'][m
[31m-    const isPaymentSuccess = successStatuses.includes(orderStatus)[m
[31m-[m
[31m-    if (!isPaymentSuccess) {[m
[31m-      console.log('[Afdian Webhook] 订单状态不是支付成功，跳过处理')[m
[31m-      return NextResponse.json({ ec: 200, em: 'success' })[m
[31m-    }[m
[31m-[m
[31m-    console.log('[Afdian Webhook] 订单支付成功，开始激活 VIP')[m
[31m-[m
[31m-    // ============================================================================[m
[31m-    // 6. 使用 Supabase Admin 客户端更新用户 VIP 状态[m
[31m-    // ============================================================================[m
[31m-    const supabase = createAdminClient()[m
[31m-[m
[31m-    // 更新用户数据[m
[31m-    const { error: updateError } = await supabase[m
[31m-      .from('users')[m
[31m-      .update({[m
[31m-        is_pro: true,[m
[31m-        subscription_status: 'active',[m
[31m-        subscription_type: 'yearly',[m
[31m-        current_period_end: new Date(Date.now() + 365 * 24 * 60 * 60 * 1000).toISOString(),[m
[31m-        points: 8888,[m
[31m-        updated_at: new Date().toISOString()[m
[31m-      })[m
[31m-      .eq('id', remark)[m
[31m-[m
[31m-    if (updateError) {[m
[31m-      console.error('[Afdian Webhook] 更新用户数据失败:', updateError)[m
[31m-      return NextResponse.json({ ec: 200, em: 'success' })[m
[32m+[m[32m    } else {[m
[32m+[m[32m      // ============================================================================[m
[32m+[m[32m      // 2. 读取并解析请求体[m
[32m+[m[32m      // ============================================================================[m
[32m+[m[32m      const body = await request.text()[m
[32m+[m[32m      let requestData[m
[32m+[m
[32m+[m[32m      try {[m
[32m+[m[32m        requestData = JSON.parse(body)[m
[32m+[m[32m      } catch (parseError) {[m
[32m+[m[32m        console.error('[Afdian Webhook] 请求体 JSON 解析失败:', parseError)[m
[32m+[m[32m      }[m
[32m+[m
[32m+[m[32m      if (requestData) {[m
[32m+[m[32m        console.log('[Afdian Webhook] 请求数据:', JSON.stringify(requestData, null, 2))[m
[32m+[m
[32m+[m[32m        // ============================================================================[m
[32m+[m[32m        // 3. 爱发电 Webhook 验签[m
[32m+[m[32m        // ============================================================================[m
[32m+[m[32m        const { ec, em, data, sign } = requestData[m
[32m+[m
[32m+[m[32m        if (!sign) {[m
[32m+[m[32m          console.error('[Afdian Webhook] 缺少签名参数')[m
[32m+[m[32m        } else {[m
[32m+[m[32m          // 按照爱发电官方文档的验签规则：[m
[32m+[m[32m          // 将 params（JSON字符串） + token 拼接后进行 MD5 加密[m
[32m+[m[32m          const paramsStr = JSON.stringify(data)[m
[32m+[m[32m          const signContent = paramsStr + afdianToken[m
[32m+[m[32m          const computedSign = crypto[m
[32m+[m[32m            .createHash('md5')[m
[32m+[m[32m            .update(signContent, 'utf-8')[m
[32m+[m[32m            .digest('hex')[m
[32m+[m
[32m+[m[32m          console.log('[Afdian Webhook] 计算的签名:', computedSign)[m
[32m+[m[32m          console.log('[Afdian Webhook] 收到的签名:', sign)[m
[32m+[m
[32m+[m[32m          if (computedSign !== sign) {[m
[32m+[m[32m          