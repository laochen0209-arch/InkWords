## 问题分析

根目录下的 `去文字.png` (2,021,507 字节) 和 public 目录下的 `去文字.png` (4,302,765 字节) 是不同的文件。页面使用的是 public 目录下的旧图片，而不是用户想要的新图片。

## 修复方案

1. 将根目录下的 `去文字.png` 复制到 `public/去文字.png`，覆盖旧文件
2. 两个页面 (classics 和 news) 的背景路径已经正确设置为 `/去文字.png`，复制后即可显示新图片

