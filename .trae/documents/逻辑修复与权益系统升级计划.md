## 一、紧急 Bug 修复

### 1.1 同步 Profile 页面 UI
- 修改 `app/profile/page.tsx`，替换 VipBanner 为 PricingCard 组件
- 如果是 VIP 用户，显示 SubscriptionStatus 卡片

### 1.2 修复支付跳转逻辑 (app/subscription/page.tsx)
- 修正 handleSubscribe 函数：调用 /api/checkout -> 获取 Stripe URL -> window.location.href 跳转
- 移除直接修改数据库状态的错误逻辑

### 1.3 完善会员状态显示
- 创建 SubscriptionStatus 组件显示会员详情
- 在 profile 和 subscription 页面显示：会员类型、到期时间、权限列表

## 二、配额与权益系统 (Freemium)

### 2.1 数据库变更
修改 `prisma/schema.prisma` 给 users 表增加：
- `study_daily_count` (int, default 0): 今日学习次数
- `library_daily_count` (int, default 0): 今日阅读次数
- `last_reset_date` (timestamp): 上次重置配额的时间
- `practice_tickets` (int, default 5): 练习券余额

### 2.2 后端逻辑

#### 2.2.1 创建配额检查工具函数
- `lib/quotas/check-quotas.ts`: 统一处理配额检查和重置逻辑

#### 2.2.2 更新 Study API
- 在 `/api/study/data` 中添加配额检查
- 非会员每日限制 5 次

#### 2.2.3 更新 Library API
- 在 `/api/articles` 中添加配额检查
- 非会员每日限制 5 次

#### 2.2.4 更新 Practice API
- 在练习相关 API 中添加练习券检查
- 非会员每次消耗 1 张练习券

### 2.3 注册福利逻辑
- 修改 `app/api/register/route.ts`
- 新用户注册时设置 `practice_tickets = 5`

### 2.4 前端拦截与反馈
- 创建 UpgradeModal 组件
- 当 API 返回 LIMIT_REACHED 时自动弹出
- 在 /practice 页面顶部显示剩余练习次数

## 三、执行顺序
1. 数据库 Schema 更新
2. 修复 subscription/page.tsx 支付逻辑
3. 创建 SubscriptionStatus 组件并更新 profile/page.tsx
4. 创建配额检查工具函数
5. 更新 Study/Library/Practice API
6. 更新注册逻辑
7. 创建 UpgradeModal 组件
8. 更新 practice 页面显示剩余次数