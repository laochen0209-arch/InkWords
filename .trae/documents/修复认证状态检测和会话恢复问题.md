## 修复计划

### 问题分析
从日志可以看出：
1. `getSession()` 第一次返回空（会话未恢复）
2. `INITIAL_SESSION` 事件触发但 session 为 undefined
3. 防抖机制可能阻止了后续的数据获取

### 修复内容

#### 1. 修复 Auth Context 中的会话恢复逻辑
**文件**: `lib/contexts/auth-context.tsx`

- 添加会话恢复等待机制
- 在 `INITIAL_SESSION` 事件中也尝试获取 session
- 添加重试逻辑

#### 2. 修复 PracticeContent 中的防抖逻辑
**文件**: `app/practice/PracticeContent.tsx`

- 修改防抖逻辑：当没有用户传入时，不记录获取时间
- 确保 `INITIAL_SESSION` 事件能触发数据获取
- 添加更详细的日志以便调试

#### 3. 确保数据获取在认证完成后执行
- 监听 `authLoading` 状态变化
- 当认证状态从 loading 变为完成时，触发数据获取

请确认这个修复方案后，我将开始实施具体的代码修改。