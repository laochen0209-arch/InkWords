## 问题分析

### 1. 数据库对接问题
代码中已经实现了数据库查询逻辑，但可能：
- `practice_records` 表不存在
- 表结构不匹配
- 没有数据

### 2. 背景图片修改
需要将以下页面的背景改为 `bg3.png`：
- `app/practice/page.tsx`
- `app/practice/mock/page.tsx`
- `app/practice/drill/page.tsx`

## 修复计划

### 第一步：修改背景图片
1. **practice/page.tsx**: 将 `bg-gradient-to-br from-[#F5F2EC] to-[#E8E4DC]` 改为 `bg3.png` 背景
2. **mock/page.tsx**: 将 `bg-slate-50` 改为 `bg3.png` 背景
3. **drill/page.tsx**: 将 `bg-slate-50` 改为 `bg3.png` 背景

### 第二步：修复数据库查询
1. 检查并修复 `practice_records` 表查询
2. 添加错误处理和回退逻辑
3. 确保即使数据库查询失败也能显示默认数据

### 第三步：添加调试信息
1. 添加 console.log 输出查询结果
2. 方便排查数据是否成功获取

## 修改文件
- app/practice/page.tsx
- app/practice/mock/page.tsx
- app/practice/drill/page.tsx