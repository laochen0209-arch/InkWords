## 修复计划

根据检查结果，需要修复以下问题：

### 1. 修复 ExamResult 表（严重）
- 添加 `examType` TEXT 字段
- 添加 `maxScore` INTEGER 字段

### 2. 修复 StudyWord 表（中等）
- 添加 `exampleZh` TEXT 字段（前端需要显示中文例句）

### 3. 创建 mock_exams 表（中等）
- 创建表用于存储 drill 功能的模拟考试数据
- 字段：id, exam_type, sections, questions, rewritten_content, createdAt, updatedAt

### 执行步骤
1. 修改 `prisma/recreate-all-tables.sql` 文件
2. 在数据库中执行 ALTER TABLE 或直接重新创建表
3. 验证修复结果