## 任务说明

用户想要用新图片替换 classics 页面的背景。

## 执行步骤

1. 将用户提供的图片保存到 `public/bg-classics.png`
2. 更新 `app/library/classics/[id]/page.tsx` 中的背景图片路径从 `/去文字.png` 改为 `/bg-classics.png`

## 图片处理

用户提供的图片是一张水墨山水画，适合作为 classics 页面的背景。需要将其保存到 public 目录下，然后更新代码引用。