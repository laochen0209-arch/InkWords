## 任务目标
将 news/[id]/page.tsx 的完整功能模板复制到 classics/[id]/page.tsx，确保两个阅读页面功能完全一致。

## 两个文件的主要差异

### news/[id]/page.tsx (第159-169行)
```tsx
if (!raw) {
  setLoading(false)
  setArticle({
    title_en: "Content Not Found",
    title_zh: "内容不存在",
    content_en: "",
    content_zh: "",
    category: "ERROR"
  })
  return
}
```

### classics/[id]/page.tsx (第159-161行)
```tsx
if (!raw) {
  setLoading(false)
  return
}
```

**结论**：news 页面有更完善的错误处理，其他部分两文件已完全一致。

## 实施方案

直接将 news/[id]/page.tsx 的完整内容写入 classics/[id]/page.tsx，确保：
1. ✅ 错误处理逻辑统一（使用 news 版本的错误提示）
2. ✅ RenderEnglish 组件（带整句朗读喇叭按钮）
3. ✅ RenderChinese 组件（带整句朗读喇叭按钮）
4. ✅ 所有样式和交互保持一致

请确认后我将执行文件替换。