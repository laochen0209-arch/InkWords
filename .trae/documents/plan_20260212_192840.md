## 任务说明

将 classics 页面的背景图片从 `/bg-classics.png` 改为 `/去文字.png`。

## 执行步骤

修改 `app/library/classics/[id]/page.tsx` 第 426 行：
- 将 `backgroundImage: 'url("/bg-classics.png")'` 改为 `backgroundImage: 'url("/去文字.png")'`

图片文件 `去文字.png` 已经存在于 `public` 目录下，可以直接使用。