## 问题分析

从截图可以看出数据库结构问题：

1. **数据库里的 `pinyin` 字段存的是英文音标** `/ˌɔːtəˈmeɪʃən/`
2. 这不是中文拼音，而是英文 pronunciation

## 正确的数据结构应该是

```typescript
interface WordItem {
  zh: string        // 中文：自动化
  en: string        // 英文：automation
  pinyin: string    // 中文拼音：zì dòng huà
  pronunciation: string  // 英文音标：/ˌɔːtəˈmeɪʃən/
}
```

## 需要修改的地方

### 1. 类型定义（第20-28行）
添加 `pronunciation` 字段

### 2. 输入框提示显示（第417-421行）
- 学中文模式：输入框提示应该显示**中文拼音**（从 pinyin 字段分割）
- 但数据库里 pinyin 存的是音标，需要确认字段映射

### 3. Pinyin 区域显示（第464-469行）
- 学中文模式：显示中文拼音
- 但数据库里存的是音标

### 4. Word Details 区域（第506-520行）
- Pinyin 字段显示的是音标，需要改为显示真正的拼音

## 解决方案

由于数据库里 `pinyin` 字段实际存的是英文音标，我需要：

1. **添加 `pronunciation` 字段到类型定义**
2. **修改显示逻辑**：
   - 学中文模式：
     - 输入框提示：需要真正的中文拼音（可能需要用其他字段或前端生成）
     - Pinyin 区域：显示中文拼音
     - Word Details：Pinyin 显示中文拼音，添加 Pronunciation 显示音标
   - 学英文模式：
     - 输入框提示：显示英文
     - Pronunciation 区域：显示音标

## 临时解决方案

由于数据库字段限制，我建议：

1. **学中文模式**：
   - 输入框提示：显示**中文汉字**（从 zh 字段取）
   - Pinyin 区域：显示**中文汉字** + 拼音（如果数据库有）
   - 或者隐藏 Pinyin 区域，改为显示 Chinese Characters

2. **学英文模式**：
   - 输入框提示：显示**英文**
   - Pronunciation 区域：显示音标（从 pinyin 字段取）

让我重新设计显示逻辑。