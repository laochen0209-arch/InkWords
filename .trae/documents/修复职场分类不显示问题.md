## 问题分析

1. 前端传递的分类是 `Professional`（正确的数据库值）
2. 数据库中 `study_words` 只有 1 条 Professional 数据（schedule）
3. 数据库中 `study_sentences` 有 2 条 Professional 数据

问题可能是：
1. API 查询没有返回数据（大小写敏感问题）
2. 前端没有正确处理返回的数据

## 修复方案

1. 修改 API 使用 `ilike` 进行不区分大小写的查询
2. 添加更多调试日志确认数据是否返回
3. 确保前端正确显示返回的数据