## 任务目标

修改备考中心页面顶部的考试类型选择器，使其在切换考试类型时同时切换语言模式：

### 语言模式映射规则

**中文考试（LEARN\_CHINESE）：**

* HSK → 切换到 LEARN\_CHINESE

* BCT → 切换到 LEARN\_CHINESE

* TOCFL → 切换到 LEARN\_CHINESE

**英文考试（LEARN\_ENGLISH）：**

* IELTS → 切换到 LEARN\_ENGLISH

* TOEFL → 切换到 LEARN\_ENGLISH

* CET-4 → 切换到 LEARN\_ENGLISH

* CET-6 → 切换到 LEARN\_ENGLISH

### 实施方案

1. 在 `handleTypeChange` 函数中：

   * 根据选择的考试类型判断应该使用哪种学习模式

   * 调用 `switchMode` 切换语言模式

   * 更新 URL 参数

2. 代码逻辑：

```tsx
const handleTypeChange = (newType: string) => {
  // 判断考试类型所属语言轨道
  const isChineseExam = ['HSK', 'BCT', 'TOCFL'].includes(newType)
  const newMode = isChineseExam ? 'LEARN_CHINESE' : 'LEARN_ENGLISH'
  
  // 切换语言模式
  switchMode(newMode)
  
  // 更新 URL
  const params = new URLSearchParams(searchParams.toString())
  params.set("type", newType)
  router.push(`/practice?${params.toString()}`)
}
```

请确认后我将实施修改。
