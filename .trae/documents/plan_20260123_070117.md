# 修正语言系统翻译内容

## 目标
确保翻译内容符合语言学习核心原则：
- **LEARN_ENGLISH 模式** → 全站 UI 显示中文（导航、按钮、提示等）
- **LEARN_CHINESE 模式** → 全站 UI 显示英文（导航、按钮、提示等）

## 修改计划

### 1. 修改 `lib/i18n.ts` 文件

#### LEARN_ENGLISH 部分（学英语，UI 全中文）
- ✅ `nav`：已经是中文（首页、修习、文库等）
- ❌ `auth`：需要改为中文（登录、注册、退出登录等）
- ❌ `dashboard`：需要改为中文（早安、每日签到、今日进度等）
- ✅ `practice`：已经是中文（修习、词语、句子等）
- ✅ `library`：已经是中文（经典文库、新闻阅读等）
- ❌ `settings`：需要改为中文（设置、语言设置、外观设置等）
- ❌ `profile`：需要改为中文（我的、会员、已学单词等）
- ❌ `subscription`：需要改为中文（开通会员、永久会员等）
- ❌ `reader`：需要改为中文（已加入生词本、字体设置、播放等）
- ❌ `common`：需要改为中文（取消、保存、关闭等）

#### LEARN_CHINESE 部分（学中文，UI 全英文）
- ❌ `nav`：需要改为英文（Home, Practice, Library 等）
- ❌ `auth`：需要改为英文（Log In, Sign Up, Logout 等）
- ❌ `dashboard`：需要改为英文（Good morning, Daily Check-in 等）
- ❌ `practice`：需要改为英文（Practice, Words, Sentences 等）
- ❌ `library`：需要改为英文（Classics, News, Novels 等）
- ❌ `settings`：需要改为英文（Settings, Language Settings 等）
- ❌ `profile`：需要改为英文（Profile, Premium, Words Learned 等）
- ❌ `subscription`：需要改为英文（Unlock Premium, Lifetime Premium 等）
- ❌ `reader`：需要改为英文（Added to Vocabulary, Font Settings 等）
- ❌ `common`：需要改为英文（Cancel, Save, Close 等）

## 验证
修改完成后，确保：
1. TypeScript 编译无错误
2. 所有 UI 文案符合语言学习原则