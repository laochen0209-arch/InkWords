# 统一语言方案 - 全站英语 UI

## 目标

将全站底层语言统一为英语，只有**学习内容**和**阅读内容**显示中文。

## 修改计划

### 1. 修改核心语言上下文 (language-context.tsx)

* **修改默认学习模式**: `LEARN_CHINESE` → `LEARN_ENGLISH`

* **简化 UI 语言逻辑**: 移除取反逻辑，直接使用 `learningMode` 作为 UI 语言

* **修改** **`getTranslation()`** **函数**: 直接返回 `TRANSLATIONS[learningMode]`

* **修改** **`uiLanguage`** **状态**: 直接设置为 `learningMode === 'LEARN_ENGLISH' ? 'zh' : 'en'`

### 2. 修改所有页面组件 (11个文件)

移除所有页面的 `uiLanguage` 计算逻辑，直接使用 `useLanguage()` 返回的 `t`

**修改列表**:

* `app/home/page.tsx`

* `app/login/page.tsx`

* `app/register/page.tsx`

* `app/settings/languages/page.tsx`

* `app/study/page.tsx`

* `app/onboarding/page.tsx`

* `app/reader/[id]/page.tsx`

* `app/profile/page.tsx`

* `components/bottom-nav-bar.tsx`

* `components/profile/settings-list.tsx`

* `components/reader/reader-toolbar.tsx`

* `components/reader/reader-content.tsx`

**修改内容**: 将 `const uiLanguage = learningMode === 'LEARN_ENGLISH' ? 'LEARN_CHINESE' : 'LEARN_ENGLISH'` 删除，直接使用 `const t = TRANSLATIONS[learningMode]`

### 3. 修改 profile/page.tsx

* 将 `const t = TRANSLATIONS[uiLanguage === "zh" ? "LEARN_CHINESE" : "LEARN_ENGLISH"]` 改为 `const t = TRANSLATIONS[learningMode]`

### 4. 修改 library-list.tsx

* 将 `const t = TRANSLATIONS[learningMode === "LEARN_CHINESE" ? "LEARN_CHINESE" : "LEARN_ENGLISH"]` 改为 `const t = TRANSLATIONS[learningMode]`

### 5. 修改 library/page.tsx 和 library-header.tsx

* 添加 `useLanguage()` hook，使用翻译显示"文库"

* 修改硬编码中文为 `t.library`

### 6. 修改 profile-header.tsx

* 将 `const t = UI_TEXT[currentLang]` 改为使用 `useLanguage()` 返回的 `t`

## 最终效果

### LEARN\_ENGLISH 模式（学英语）

* ✅ 全站 UI 显示英文（导航、按钮、提示）

* ✅ 学习页面：题目显示英文，辅助解析显示中文

* ✅ 阅读页面：内容显示中文

### LEARN\_CHINESE 模式（学中文）

* ✅ 全站 UI 显示中文（导航、按钮、提示）

* ✅ 学习页面：题目显示中文，辅助解析显示英文

* ✅ 阅读页面：内容显示英文

## 优势

* 🎯 **逻辑统一**: 所有组件使用相同的语言逻辑

* 🎯 **易于维护**: 只需修改 `language-context.tsx` 即可调整全局语言

* 🎯 **消除混乱**: 移除所有不一致的取反逻辑

