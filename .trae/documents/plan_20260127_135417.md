将三段代码拼接成完整的 drill 页面代码，然后覆盖写入 `app/practice/drill/page.tsx`：

1. **第 1 段**：引用、接口、组件和状态定义
2. **第 2 段**：fetchExam 随机逻辑、handleSubmit 提交逻辑
3. **补全**：loading 和空状态的完整 JSX 结构
4. **第 3 段**：完整的 UI 渲染（导航 + 文章区 + 答题区 + 交互）

**核心功能**：

* 随机获取试卷（count + randomOffset）

* 文章纯文本显示（`<p>` 标签，`text-slate-700`）

* 完整答题交互（选择、提交、反馈）

* 正确/错误状态显示（绿色/红色高亮 + 图标）

* 解析显示

* 下一题功能

