## 问题分析

1. **日期显示英文**：`formatExpiryDate` 函数逻辑反了，`isLearnChinese` 为 `true` 时显示英文，应该显示中文
2. **订阅类型显示月度**：API 可能返回了错误的类型，或者 `STRIPE_YEARLY_PRICE_ID` 环境变量未设置

## 修复方案

### 1. 修复日期格式函数
修改 `formatExpiryDate`，当 `isLearnChinese` 为 `true` 时显示中文（因为用户在学习中文，界面应该是中文）

### 2. 简化订阅类型判断
修改 API 路由，使用更可靠的方式判断订阅类型（根据订阅间隔而不是价格ID）

### 3. 添加调试日志
在 profile 页面添加日志，查看 API 返回的数据

确认后立即执行修复。