## 任务目标
在每段句子结尾添加喇叭图标，点击可朗读整句（中英都加，两个模式版本都加）。

## 需要修改的文件

### 1. app/library/news/[id]/page.tsx
- **RenderEnglish 组件** (第32-76行)：在段落末尾添加喇叭图标按钮，点击朗读整句英文
- **RenderChinese 组件** (第78-100行)：在段落末尾添加喇叭图标按钮，点击朗读整句中文

### 2. app/library/classics/[id]/page.tsx
- **RenderEnglish 组件** (第32-76行)：同上修改
- **RenderChinese 组件** (第78-100行)：同上修改

## 具体修改内容

### RenderEnglish 修改：
```tsx
// 当前：只返回 <p> 元素
// 修改后：返回 div 包裹，包含 <p> 和喇叭按钮
<div className="flex gap-4 items-start">
  <p className="flex-1">...</p>
  <button onClick={() => speak(text, 'en-US')}>
    <Volume2 />
  </button>
</div>
```

### RenderChinese 修改：
```tsx
// 当前：只返回 <p> 元素
// 修改后：返回 div 包裹，包含 <p> 和喇叭按钮
<div className="flex gap-4 items-start">
  <p className="flex-1">...</p>
  <button onClick={() => speak(text, 'zh-CN')}>
    <Volume2 />
  </button>
</div>
```

## 样式统一
- 喇叭按钮样式与现有 classics 页面中文段落一致：
  - `w-10 h-10 flex-shrink-0 flex items-center justify-center rounded-full bg-white border border-stone-200 text-stone-400 hover:border-[#C23E32] hover:text-[#C23E32] active:scale-95 transition-all`

请确认此方案后，我将开始实施具体的代码修改。