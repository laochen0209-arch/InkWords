## 任务分析

1. **删除 `/checkin` 页面** - 保留 `/check-in` 作为唯一签到页面
2. **检查 `/check-in` 数据交互** - 它已经在使用正确的 API：
   - `/api/user/me` - 获取用户数据
   - `/api/checkin` - 签到功能
3. **更新导航链接** - 确保底部导航栏指向 `/check-in`

## 涉及文件
- 删除 `app/checkin/page.tsx`
- 检查 `components/library/bottom-nav-bar.tsx` 中的导航链接

## 数据关联说明
`/check-in` 页面已经与整个网站数据关联：
- 使用相同的 `localStorage` key (`inkwords_user`)
- 调用相同的 API 端点
- 数据与其他页面共享