## 需求分析

用户希望：
1. 按回车键确认答案
2. 如果答案错误，显示**红色下划线 + 抖动效果**
3. 继续按回车，保持抖动效果

## 修复方案

### 1. 添加抖动动画 CSS
在输入框的 className 中添加抖动动画类

### 2. 添加 shake 状态
添加 `isShaking` 状态来控制抖动动画的触发

### 3. 修改 handleCheck 函数
- 当答案错误时，设置 `isShaking` 为 true
- 触发抖动动画
- 同时更新 `inputErrors` 显示具体哪个单词错了

### 4. 修改全局键盘监听
- 错误状态下按回车，重新触发抖动
- 不自动跳转，直到答案正确

### 5. 视觉效果
- 错误单词：红色下划线 + 抖动动画
- 使用 Tailwind 的 `animate-bounce` 或自定义 CSS 抖动

请确认这个修复方案后，我将开始实施具体的代码修改。