## 修复计划

### 问题 1: Supabase 表名大小写
- 修改 `/app/api/study/data/route.ts`
- 将 `.from('StudyWord')` 改为 `.from('study_words')`
- 将 `.from('StudySentence')` 改为 `.from('study_sentences')`

### 问题 2: localStorage 键名不一致
- 修改 `/app/study/page.tsx`
- 从 `localStorage.getItem('userId')` 改为解析 `inkwords_user`

### 问题 3: Prisma Schema 表名映射
- 检查并更新 `prisma/schema.prisma`
- 为所有模型添加 `@@map()` 映射到 snake_case 表名

### 执行步骤
1. 修复 API 路由中的 Supabase 表名
2. 修复前端 localStorage 读取逻辑
3. 更新 Prisma schema 添加表名映射