## 优化计划

### 第一阶段：后端数据模型与API

1. **更新 Prisma Schema**
   - 添加 `CheckIn` 模型记录每日签到
   - 包含字段：id, userId, date, points, createdAt

2. **创建签到 API**
   - `GET /api/checkin` - 获取用户签到状态、连续天数、本周签到记录
   - `POST /api/checkin` - 执行签到操作

3. **更新用户 API**
   - 扩展 `/api/user/me` 返回今日学习统计

### 第二阶段：前端优化

1. **移除假数据**
   - 删除 `streak: 12` 硬编码
   - 删除 `45 分钟` 和 `75%` 硬编码
   - 删除 `checkStatus: [true, true, true, false]` 硬编码
   - 删除 `"修行者"` 硬编码用户名

2. **连接真实数据**
   - 从 `/api/user/me` 获取用户名称
   - 从 `/api/checkin` 获取签到状态、连续天数
   - 从 `UserStudyStats` 获取今日学习时长
   - 根据真实数据计算本周签到状态

3. **UI 优化**
   - 添加加载状态
   - 优化卡片间距和视觉层次
   - 改进签到按钮交互反馈
   - 添加错误处理

### 第三阶段：测试验证

1. 验证签到功能正常工作
2. 验证数据持久化
3. 验证UI响应式布局

---

请确认此计划后，我将开始实施具体的代码修改。