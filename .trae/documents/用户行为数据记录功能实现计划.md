## 任务概述
在关键用户行为节点调用 Supabase 写入 user_activities 表，记录用户的核心行为数据。

## 已分析的代码结构

### 1. 现有 Supabase 配置
- 文件: `lib/supabase.ts`
- 当前仅导出 supabase 客户端实例

### 2. 文章阅读页面
- 文件: `app/article/[id]/page.tsx` - 通用文章详情页
- 文件: `app/reader/[id]/page.tsx` - 古诗词阅读页
- 文件: `components/reader/article-reader.tsx` - 文章阅读器组件

### 3. 考试页面
- 文件: `app/practice/drill/ExamContent.tsx`
- 已有 `handleSubmit` 函数（第 441 行）用于提交试卷

### 4. 单词学习页面
- 文件: `app/vocabulary/page.tsx` - 生词本页面
- 文件: `components/vocabulary/vocabulary-card.tsx` - 单词卡片组件
- 当前只有 `onRemove` 回调，需要添加"标记为已掌握"功能

## 实施步骤

### 步骤 1: 在 lib/supabase.ts 中封装 logUserActivity 函数
```typescript
/**
 * 记录用户活动到 user_activities 表
 * @param action_type - 活动类型: 'read_article' | 'take_exam' | 'learn_word'
 * @param target_id - 目标ID（文章ID/试卷ID等，可选）
 * @param details - 活动详情对象
 */
export async function logUserActivity(
  action_type: string,
  target_id?: string,
  details?: Record<string, any>
): Promise<void>
```

### 步骤 2: 在文章阅读页面添加阅读完成记录
- **app/article/[id]/page.tsx**: 添加 30 秒计时器，自动记录阅读完成
- **app/reader/[id]/page.tsx**: 同上
- 记录数据: `action_type: 'read_article'`, `target_id: 文章ID`, `details: { title, category }`

### 步骤 3: 在考试提交时记录
- **app/practice/drill/ExamContent.tsx**: 在 `handleSubmit` 函数中添加记录
- 记录数据: `action_type: 'take_exam'`, `target_id: 试卷ID`, `details: { score, exam_type, total_questions }`

### 步骤 4: 在单词学习页面添加"标记为已掌握"功能
- **components/vocabulary/vocabulary-card.tsx**: 添加"标记为已掌握"按钮
- **app/vocabulary/page.tsx**: 添加 `onMaster` 回调，调用 logUserActivity
- 记录数据: `action_type: 'learn_word'`, `details: { word }`

## 数据库表结构 (user_activities)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| user_id | uuid | 用户ID (外键) |
| action_type | varchar | 活动类型 |
| target_id | varchar | 目标ID |
| details | jsonb | 详情 |
| created_at | timestamp | 创建时间 |

## 文件修改清单
1. `lib/supabase.ts` - 添加 logUserActivity 函数
2. `app/article/[id]/page.tsx` - 添加阅读完成记录
3. `app/reader/[id]/page.tsx` - 添加阅读完成记录
4. `app/practice/drill/ExamContent.tsx` - 添加考试提交记录
5. `components/vocabulary/vocabulary-card.tsx` - 添加标记已掌握按钮
6. `app/vocabulary/page.tsx` - 添加单词学习记录

请确认此计划后，我将开始实施具体的代码修改。