## 问题分析

1. **显示"月度会员"而不是"年度会员"**：
   - `planType` 从 `/api/subscription/type` 获取，可能返回 "monthly"
   - 需要检查数据库中的订阅类型

2. **时间显示英文**：
   - `formatExpiryDate` 函数逻辑反了
   - `isLearnChinese` 为 true 时应该显示中文，false 时显示英文

3. **时间不正确（2125年）**：
   - 数据库中的测试数据问题

## 修复方案

### 1. 修复 Profile 页面的时间格式化逻辑
修改 `formatExpiryDate` 函数：
```typescript
// 当前逻辑（错误）：
return date.toLocaleDateString(isLearnChinese ? "en-US" : "zh-CN", ...)

// 应该改为：
return date.toLocaleDateString(isLearnChinese ? "zh-CN" : "en-US", ...)
```

### 2. 检查 `/api/subscription/type` API
查看该 API 如何获取订阅类型

### 3. 修改 SubscriptionStatus 组件
确保正确显示会员类型

请确认后执行修改。