
## 完整修复计划

### 问题 1：VIP 检查不包含到期时间
**修复位置**：
- `lib/quotas/check-quotas.ts` - 更新 `isUserVip` 函数，添加到期时间检查
- `lib/contexts/auth-context.tsx` - 更新 `checkIsVip` 函数，添加到期时间检查
- 其他地方的 VIP 检查逻辑也需要同步更新

### 问题 2：积分兑换功能未实现
**修复内容**：
1. **创建积分兑换后端 API** - `app/api/redeem/route.ts`
   - 验证用户身份
   - 检查积分是否足够（&gt;= 100）
   - 扣除 100 积分
   - 计算并设置会员到期时间（1天）
   - 正确处理现有会员（延期 vs 新增）
   - 记录用户活动日志

2. **修改 Profile 页面** - `app/profile/page.tsx`
   - 更新 `handleRedeem` 函数，调用真实 API
   - 添加加载状态
   - 兑换成功后刷新用户会话
   - 显示成功/失败提示

3. **确保会员到期自动失效**
   - 所有 VIP 检查函数都同时检查 `subscription_status` **和** `current_period_end`
