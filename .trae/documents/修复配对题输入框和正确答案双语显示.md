## 问题分析

从截图可以看到：
1. 配对题（matching）只显示了选项列表，没有输入框让用户输入答案
2. 正确答案显示的是 JSON 字符串 `{"Paris": "Haussmann renovation", ...}`，需要格式化成可读的双语格式

## 修复方案

### 1. 添加配对题输入框
为 matching 类型添加文本输入框，让用户可以输入答案：

```javascript
{currentQ.type === 'matching' && (
  <div className="space-y-3">
    <div className="p-3 bg-blue-50 text-blue-800 text-sm rounded-lg mb-4">
      Please match the following pairs (enter your answer in the box below):
    </div>
    {Array.isArray(currentQ.options) && currentQ.options.map((opt, idx) => (
      <div key={idx} className="p-4 bg-white border-2 border-stone-200 rounded-xl shadow-sm text-stone-700 font-medium">
        {opt}
      </div>
    ))}
    {/* 添加输入框 */}
    <textarea 
      className="w-full p-4 border-2 border-stone-200 rounded-xl h-32 outline-none focus:border-[#C23E32] focus:ring-2 focus:ring-[#C23E32]/10 transition-all text-base leading-relaxed resize-none bg-white/50 font-serif mt-4"
      placeholder="Enter your answer here (e.g., Paris - Haussmann renovation, New York - Grid system...)" 
      value={selectedOption || ''} 
      onChange={(e) => onOptionSelect(e.target.value)} 
      disabled={showResult} 
    />
  </div>
)}
```

### 2. 格式化正确答案显示
修改正确答案渲染逻辑，如果是 JSON 格式的配对答案，解析并格式化成双语显示：

```javascript
// 解析正确答案
const renderCorrectAnswer = () => {
  const answer = currentQ.correct_answer;
  
  // 尝试解析 JSON 格式的配对答案
  if (answer && answer.startsWith('{')) {
    try {
      const pairs = JSON.parse(answer);
      return (
        <div className="space-y-2">
          {Object.entries(pairs).map(([key, value], idx) => (
            <div key={idx} className="flex items-center gap-2">
              <span className="font-medium">{key}</span>
              <span className="text-stone-400">→</span>
              <span>{String(value)}</span>
            </div>
          ))}
        </div>
      );
    } catch {
      // 解析失败，显示原文
      return <strong>{answer}</strong>;
    }
  }
  
  // 普通答案
  return <strong>{answer || 'N/A'}</strong>;
};
```

### 3. 同时显示中文翻译
如果有 `correct_answer_cn` 字段，同样解析并显示中文配对。

## 修改位置
- 文件：app/practice/drill/[examId]/page.tsx
- matching 类型渲染块（约第 96 行）
- 正确答案显示部分（约第 121 行）