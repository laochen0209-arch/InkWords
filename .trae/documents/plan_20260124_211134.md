# 修复经典阅读页面后退按钮链接

## 修改文件
- `app/library/classics/[id]/page.tsx`

## 修改内容
将第136-143行的后退按钮链接从 `/library` 改为 `/`（首页）

## 修改详情
- 修改 `Link` 组件的 `href` 属性从 `"/library"` 改为 `"/"`
- 更新按钮文本从"返回文库"改为"返回首页"（中文）/ "Back to Home"（英文）

## 预期效果
点击后退按钮后，用户将回到欢迎页（首页），而不是文库列表页