将两段代码拼接成完整的 mock 页面代码，然后覆盖写入 `app/practice/mock/page.tsx`：

1. **第一段**：导入、接口、组件逻辑、数据获取
2. **补全**：loading 状态和空状态的完整 JSX 结构
3. **第二段**：正常渲染（顶部导航 + 左侧文章区 + 右侧答题区）

**核心保证**：

* 文章使用纯文本 `<p>` 标签（`text-slate-700 font-serif`）

* 简单 `split('\n')` 段落分割

* 绝对不使用 input 或下划线特效

* 使用正确的 `@/lib/supabase` 引用

